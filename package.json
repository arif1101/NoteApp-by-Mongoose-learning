{
  "scripts": {
    "dev" : "ts-node-dev --respawn --transpile-only src/server.ts"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.15.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "ts-node-dev": "^2.0.0"
  }
}
